/**
 * Central site configuration.
 */

export const siteConfig = {
  name: "Nearr",
  url: "https://nearr.app", // TODO: confirm production domain before deploy
  description:
    "Nearr turns the places you see in Instagram, TikTok, and Facebook videos into a real map you can visit. Share a video and Vayrin, Nearr's place-finder, tracks down the location — then Nearr saves it and reminds you when you're nearby.",
};

/**
 * App Store CTA target — the live Nearr listing.
 */
export const APP_STORE_URL =
  "https://apps.apple.com/us/app/nearr/id6764170112";

/**
 * Support contact.
 *
 * Nearr doesn't have a domain-backed inbox yet, so the founder's personal
 * address is the canonical public support contact until one exists. This
 * matches the canonical legal repo (github.com/AndrewMahran7/nearr-legal).
 */
export const SUPPORT_EMAIL = "andrew.mahran@icloud.com";
